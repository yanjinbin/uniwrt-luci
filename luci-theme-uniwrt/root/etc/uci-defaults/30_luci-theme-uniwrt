#!/bin/sh

# Keep UniWRT in the selectable theme list, but never force it as the
# active LuCI theme during install or upgrade.
uci -q set luci.themes.UniWRT=/luci-static/uniwrt
uci -q commit luci
rm -f /tmp/luci-indexcache /tmp/luci-modulecache 2>/dev/null

exit 0
